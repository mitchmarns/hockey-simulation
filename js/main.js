console.log("Hockey Simulation Loaded!");

// Example function to test
function initializeTeams() {
    let teams = ["Rangers", "Devils", "Islanders", "Sabres"];
    console.log("Teams:", teams);
}

// Call the function on load
window.onload = function() {
    initializeTeams();
};
